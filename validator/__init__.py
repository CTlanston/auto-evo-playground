"""Heterologous validator — Gemini 2.5 Pro judges Claude's work.

This package is a CODEOWNERS-protected control-plane path. Agents may NOT
modify it (see CLAUDE.md rule 6). The validator MUST NEVER receive source
code content — only behavioral evidence (contract, done.md, file lists,
test-results summaries). See docs/upgrade-plan.md §5.
"""
