# Plan

1. [code] Create `src/mathx.py` with `add(a, b)` and `mul(a, b)` functions for integer arithmetic.
2. [code] Create `tests/test_mathx.py` with pytest tests covering `add(2,3)==5`, `add(-2,3)==1`, `mul(4,5)==20`, `mul(-2,3)==-6`.
3. [rw] Add a `mathx` usage note to `README.md` mentioning both `add` and `mul`.
